@page
@model TeknoRoma.Razorpage.Pages.Subeler.IndexModel
@{
    ViewData["Title"] = "Faturalar";
    Layout = "/Pages/Shared/_Layout.cshtml";
}

<div class="card">
    <h5 class="card-header">
        <a asp-page="Create" class="btn btn-primary">Add New Sube</a>
    </h5>
    <div class="">
        <table class="table table-hover">
            <thead>
                <tr>
                    <th>SubeAdi</th>
                    <th> Fatura Tarihi</th>
                    <th>ToplamTutar</th>
                    <th>KDV</th>
                </tr>
            </thead>
            <tbody class="table-border-bottom-0">
                @foreach (var sube in Model.Subeler)
                {
                    <tr>
                        <td><strong>@sube.SubeAdi</strong></td>
                        <td><strong>@sube.Sehir</strong></td>
                        <td><strong>@sube.Ilce</strong></td>
                        <td><strong>@sube.Address</strong></td>
                        <td>
                            @{
                                if (sube.SubeAdi != null)
                                {
                                    <strong>@sube.SubeAdi</strong>

                                }
                            }
                        </td>

                        <td>
                            <div class="dropdown">
                                <button type="button" class="btn p-0 dropdown-toggle hide-arrow" data-bs-toggle="dropdown">
                                    <i class="bx bx-dots-vertical-rounded"></i>
                                </button>
                                <div class="dropdown-menu">
                                    <a class="dropdown-item" asp-page="Update" asp-route-id="@sube.Id"><i class="bx bx-edit-alt me-1"></i> Edit</a>
                                    <a class="dropdown-item" asp-page="Delete" asp-route-id="@sube.Id"><i class="bx bx-trash me-1"></i> Delete</a>
                                </div>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

