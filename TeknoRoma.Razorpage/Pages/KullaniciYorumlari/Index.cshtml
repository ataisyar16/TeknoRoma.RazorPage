@page
@model TeknoRoma.Razorpage.Pages.KullaniciYorumlari.IndexModel
@{
    ViewData["Title"] = "Faturalar";
    Layout = "/Pages/Shared/_Layout.cshtml";
}

<div class="card">
    <h5 class="card-header">
        <a asp-page="Create" class="btn btn-primary">Add New Kullanici Yorum</a>
    </h5>
    <div class="">
        <table class="table table-hover">
            <thead>
                <tr>
                    <th>KullaniciId</th>
                    <th> StokId</th>
                    <th>YorumMetni</th>
                    <th>YorumTarihi</th>
                </tr>
            </thead>
            <tbody class="table-border-bottom-0">
                @foreach (var kullaniciYorum in Model.KullaniciYorumlari)
                {
                    <tr>
                        <td><strong>@kullaniciYorum.KullaniciId</strong></td>
                        <td><strong>@kullaniciYorum.StokId</strong></td>
                        <td><strong>@kullaniciYorum.YorumMetni</strong></td>
                        <td><strong>@kullaniciYorum.YorumTarihi</strong></td>
                        <td>
                            @{
                                if (kullaniciYorum.KullaniciId != null)
                                {
                                    <strong>@kullaniciYorum.KullaniciId</strong>

                                }
                            }
                        </td>

                        <td>
                            <div class="dropdown">
                                <button type="button" class="btn p-0 dropdown-toggle hide-arrow" data-bs-toggle="dropdown">
                                    <i class="bx bx-dots-vertical-rounded"></i>
                                </button>
                                <div class="dropdown-menu">
                                    <a class="dropdown-item" asp-page="Update" asp-route-id="@kullaniciYorum.Id"><i class="bx bx-edit-alt me-1"></i> Edit</a>
                                    <a class="dropdown-item" asp-page="Delete" asp-route-id="@kullaniciYorum.Id"><i class="bx bx-trash me-1"></i> Delete</a>
                                </div>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
