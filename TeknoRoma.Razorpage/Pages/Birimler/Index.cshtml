@page
@model TeknoRoma.Razorpage.Pages.Birimler.IndexModel
@{
    ViewData["Title"] = "Birimler";
    Layout = "/Pages/Shared/_Layout.cshtml";
}

<div class="card">
    <h5 class="card-header">
        <a asp-page="Create" class="btn btn-primary">Add New Menu</a>
    </h5>
    <div class="">
        <table class="table table-hover">
            <thead>
                <tr>
                    <th>Birim Kodu</th>
                    <th>Aciklama</th>
                </tr>
            </thead>
            <tbody class="table-border-bottom-0">
                @foreach (var birim in Model.Birimler)
                {
                    <tr>
                        <td><strong>@birim.BirimKodu</strong></td>
                        <td><strong>@birim.Aciklama</strong></td>
                        <td>
                            @{
                                if (birim.BirimKodu != null)
                                {
                                    <strong>@birim.BirimKodu</strong>

                                }
                            }
                        </td>

                        <td>
                            <div class="dropdown">
                                <button type="button" class="btn p-0 dropdown-toggle hide-arrow" data-bs-toggle="dropdown">
                                    <i class="bx bx-dots-vertical-rounded"></i>
                                </button>
                                <div class="dropdown-menu">
                                    <a class="dropdown-item" asp-page="Update" asp-route-id="@birim.Id"><i class="bx bx-edit-alt me-1"></i> Edit</a>
                                    <a class="dropdown-item" asp-page="Delete" asp-route-id="@birim.Id"><i class="bx bx-trash me-1"></i> Delete</a>
                                </div>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>