@page
@model TeknoRoma.Razorpage.Pages.FaturaDetaylar.IndexModel
@{
    ViewData["Title"] = "Faturalar";
    Layout = "/Pages/Shared/_Layout.cshtml";
}

<div class="card">
    <h5 class="card-header">
        <a asp-page="Create" class="btn btn-primary">Add New Fatura Detay</a>
    </h5>
    <div class="">
        <table class="table table-hover">
            <thead>
                <tr>
                    <th>Cari Id</th>
                    <th> Fatura Tarihi</th>
                    <th>ToplamTutar</th>
                    <th>KDV</th>
                </tr>
            </thead>
            <tbody class="table-border-bottom-0">
                @foreach (var faturaDetay in Model.FaturaDetaylar)
                {
                    <tr>
                        <td><strong>@faturaDetay.FaturaId</strong></td>
                        <td><strong>@faturaDetay.StokId</strong></td>
                        <td><strong>@faturaDetay.Miktar</strong></td>
                        <td><strong>@faturaDetay.Fiyat</strong></td>
                        <td>
                            @{
                                if (faturaDetay.FaturaId != null)
                                {
                                    <strong>@faturaDetay.FaturaId</strong>

                                }
                            }
                        </td>

                        <td>
                            <div class="dropdown">
                                <button type="button" class="btn p-0 dropdown-toggle hide-arrow" data-bs-toggle="dropdown">
                                    <i class="bx bx-dots-vertical-rounded"></i>
                                </button>
                                <div class="dropdown-menu">
                                    <a class="dropdown-item" asp-page="Update" asp-route-id="@faturaDetay.Id"><i class="bx bx-edit-alt me-1"></i> Edit</a>
                                    <a class="dropdown-item" asp-page="Delete" asp-route-id="@faturaDetay.Id"><i class="bx bx-trash me-1"></i> Delete</a>
                                </div>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

